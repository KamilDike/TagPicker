import {StyleSheet} from 'react-native';
import {Values} from './styles/styles.ts';

export const AppStyles = StyleSheet.create({
  container: {
    padding: Values.defaultPadding * 2,
  },
  contentContainer: {
    marginBottom: Values.defaultMargin * 3,
  },
});
